@model 作業客戶管理.Models.ViewModel.客戶銀行資訊VM.客戶銀行資訊SearchViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@section breadcrumb{
    <li>客戶銀行資訊</li>
}
<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{
    @*<table>
            <tr>
                <td>
                    <label>銀行名稱：</label>
                    @Html.EditorFor(p => p.BankName)
                    <input type="submit" name="submit" value="搜尋" class="text-right" />
                    <a href="@Url.Action("ExcelFile", "客戶銀行資訊")" role="button" class="btn btn-group-sm">匯出成Excel</a>
                </td>
            </tr>
        </table>*@
    <div class="container">
        <div class="row-fluid">
                <div class="col-md-5">
                    銀行名稱：@Html.EditorFor(p => p.BankName)
                </div>
                    <input type="submit" name="submit" value="搜尋" class="text-right" />
                    <a href="@Url.Action("ExcelFile", "客戶銀行資訊")" role="button" class="btn btn-group-sm">匯出成Excel</a>
            </div>
        </div>
}
<table class="table">
    <tr>
        <th>銀行名稱</th>
        <th>銀行代碼</th>
        <th>分行代碼</th>
        <th>帳戶名稱</th>
        <th>帳戶號碼</th>
        <th>客戶名稱</th>
        <th>功能列表</th>
    </tr>

    @foreach (var item in Model.客戶銀行資訊列表)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.銀行名稱)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.銀行代碼)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.分行代碼)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.帳戶名稱)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.帳戶號碼)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.客戶資料.客戶名稱)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
